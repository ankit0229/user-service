# class JWTError(Exception):
#     """Exception raised for JWT-related errors."""
#     def __init__(self, message="JWT decoding failed", status_code=401):
#         self.message = message
#         self.status_code = status_code
#         super().__init__(self.message)
